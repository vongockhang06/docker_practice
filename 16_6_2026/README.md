Second exercise for docker => Trying to run many containers at the same time with .yml file
docker env_file to run all containers with env var
docker rm $(docker ps -a -q) for removing all containers

