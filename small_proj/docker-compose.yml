services:
  db:
    image: postgres:15
    env_file:
      - "../.env"
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
  app:
    build: .
    depends_on:
      - db
    env_file:
      - "../.env"
    environment:
      - DB_HOST=db

volumes:
  pgdata:
    